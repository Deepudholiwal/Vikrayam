# Database Configuration
DB_HOST=localhost
DB_NAME=classifieds
DB_USER=root
DB_PASS=your_password_here

# Application Settings
APP_URL=http://localhost
APP_ENV=production

# Session Security (set to true if using HTTPS)
SESSION_SECURE=false

